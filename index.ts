//######################################################################
//###########################           SSTech       ###################
//###########################   Programming Courses  ###################
//#####################################################################
let A, index: number;
A = parseInt(prompt('entrer un nombre'));
for (index = 1; index <= 100; index++) {
  console.log(A, '*', index, '=', index * A);
}
